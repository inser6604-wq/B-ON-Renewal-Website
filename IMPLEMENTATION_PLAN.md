# B&ON Universe 구현 계획

## 1. 문서 상태와 목적

현재는 **구현 전 기획 및 파일 구성 설계 단계**다. 이 문서는 `README.md`, `design.md`, `PRD.md`, `TRD.md`를 기준으로 향후 구현 범위와 완료 조건을 정한다. 이번 작업의 산출물은 이 문서와 `FILE_STRUCTURE.md`뿐이며, 아래 구현 항목은 모두 추후 진행 예정이다.

목표는 **포트폴리오 신뢰 형성 → 서비스 이해 → 프로젝트 문의 전환**이다. 상품 판매 UI보다 브랜드의 Shopify·디자인·글로벌 커머스 역량을 명확하게 전달한다.

현재 저장소에는 기준 문서, 원본 HTML, `images`가 있으며 Dawn 테마 디렉터리는 아직 없다. 향후 개발용 Dawn을 도입하고 프리뷰 테마에서 구현·검수한다. 라이브 테마를 직접 수정하지 않는다. 기존 문서, 원본 HTML, `images` 및 내부 파일은 삭제·이동·이름 변경·수정하지 않는다.

## 2. 구현 원칙과 디자인 기준

| 항목 | 적용 원칙 |
| --- | --- |
| 기반 | Shopify Online Store 2.0 / Dawn, Liquid + JSON template + CSS + Vanilla JavaScript |
| 분리 | 커스텀 스타일과 동작은 `bnon-base.css`, `bnon-interactions.js`로 분리하고 클래스·데이터 속성에 `bnon-` 접두어 사용 |
| 영문 폰트 | Archivo Bold/ExtraBold: 대제목·숫자·탭, Medium/Regular: 보조 라벨 |
| 한글 폰트 | Pretendard Bold/ExtraBold: 제목·강조, SemiBold/Regular: 본문·UI |
| Black | `#0A0A0A`: 본문, 다크 섹션 |
| White | `#FFFFFF`: 밝은 배경, 다크 섹션 텍스트 |
| Gray | `#F5F2F2`: Hero와 Header, 전환 배경 |
| Blue CTA | `#3154EE`: 프로젝트 문의 버튼, 제출 버튼, 서비스 활성 라벨 |
| Mint active point | `#28DBC4`: 활성 탭 3px 밑줄, Hero 마지막 점, 제한적인 강조 |
| 로고 | 공식 로고 추가 전까지 `B&ON UNIVERSE` 텍스트 로고 사용 예정 |
| 레이아웃 | Desktop 1440px 시안, 콘텐츠 약 1312px, 좌우 약 64px, Header 높이 120px을 출발점으로 유동화 |
| 타이포 크기 | 메뉴 18px, 로고 22px, Hero 제목 110px, 키워드 30px, CTA 26~30px은 데스크톱 시각 기준이며 `clamp()`로 조절 |
| 모션 | UI 250~500ms, 섹션 전환 700~1200ms를 기준으로 하되 정보 접근과 사용자 제어 우선 |

이미지·문구·링크·반복 항목은 Theme Editor에서 관리한다. 디자인 수치는 고정 화면 구현을 위한 절대값으로 쓰지 않고 grid, flex, 유동 여백으로 재현한다. 자산 누락이나 JavaScript 실패 시에도 주요 정보와 문의 동선이 남도록 설계한다.

## 3. Dawn 커스텀 범위

- Dawn의 기본 레이아웃, SEO 메타데이터, Shopify 필수 출력, 앱 연동 지점과 기본 상거래 템플릿은 보존한다. 상품·장바구니·결제 기능 재설계는 이번 리뉴얼 범위가 아니다.
- 향후 `layout/theme.liquid`에는 전용 CSS/JS 로드와 페이지당 공통 프로젝트 모달 1개를 연결하는 최소 변경만 검토한다. 실제 변경 전 Dawn 버전과 기존 로딩 구조를 확인한다.
- Header/Footer는 Dawn section group을 유지하면서 전용 B&ON 섹션을 연결하는 방향으로 설계한다. 기업 사이트 메뉴와 문의 CTA를 노출하고 기본 쇼핑몰 UI 노출은 설정·그룹 구성 수준에서 조절한다. Dawn 원본 섹션은 삭제하지 않는다.
- Home, Portfolio, Service, Contact는 전용 JSON template과 B&ON section으로 구성한다. About은 구조와 편집 가능한 블록을 먼저 준비하고 최종 콘텐츠를 기다린다.
- 기존 `templates/index.json`이 Dawn 도입 시 존재하면 백업·차이를 확인한 뒤 프리뷰에서 구성 변경한다. 현재는 어떤 테마 파일도 생성·수정하지 않는다.

## 4. 전체 구현 순서와 단계별 완료 조건

구현 착수 전 개발 스토어·프리뷰 테마, Dawn 버전, Figma 접근 및 자산 사용 가능 여부를 확인한다. 다음 순서를 유지하며 각 단계의 완료 조건을 검수한다.

| 순서 | 작업 | 완료 조건 |
| --- | --- | --- |
| 1 | 공통 디자인 토큰/폰트 | Archivo/Pretendard의 필요한 굵기와 대체 폰트 정의, 색상·여백·타이포·모션 토큰 정리, 폰트 실패 시에도 텍스트 노출, `bnon-` 스타일이 Dawn 영역에 불필요하게 번지지 않음 |
| 2 | Header/Footer | 텍스트 로고, PORTFOLIO / ABOUT / SERVICE / CONTACT 메뉴, 블루 문의 CTA, 모바일 메뉴와 키보드 닫기·포커스 복귀, Footer 링크와 회사 정보 편집 가능 |
| 3 | Home Hero | 회색 배경, `YOUR BRAND’S UNIVERSE STARTS HERE.`와 민트 점, 서비스 키워드·설명·두 CTA 구현, Portfolio 앵커와 Contact 이동 동작, 우측 애니메이션 실패 시 대체 이미지 노출 |
| 4 | Main Portfolio | 공개된 최대 6개 탭의 추가·삭제·순서 변경 가능, 선택 시 배경·목업·정보 동기화, 활성 민트 밑줄, 키보드·터치 조작, 자동 탭 전환 없음 |
| 5 | What We Do | `WHAT WE DO` / `FROM STORE TO GLOBAL.`과 5개 서비스 구성, 한 항목 열림 아코디언, 열린 블루 라벨·다크 상세, 이미지·태그 3개·링크 편집 및 접근 가능 |
| 6 | Process | 6단계 블록, 화면 중앙 단계 감지와 직접 선택 버튼 제공, 이미지·설명·진행 바가 같은 활성 상태를 사용, 모바일 버튼 조작 및 reduced motion 동작 확인 |
| 7 | Our Universe | 제공 이미지 기반 궤도 배치, 360도 회전과 정지 제어, 텍스트·CTA 가림 없음, 모바일·reduced motion에서 정적 클러스터 표시 |
| 8 | FAQ/Contact CTA | 질문·답변 블록 관리, 접근 가능한 아코디언, 프로젝트 문의와 카카오톡 실시간 문의의 목적·링크 구분, 모든 링크 편집 가능 |
| 9 | Portfolio 페이지 | 상단 갤러리 선택 강조, 전체 그리드 크기·순서·공개 설정 반영, 모든 프로젝트 카드에서 공통 모달 열기, 긴 캡처·정지·수동 보기·닫기·포커스 복귀 검증 |
| 10 | Service 페이지 | 5종 서비스의 영문명·한글명·설명·태그 3개·이미지·상세 링크 편집, Desktop sticky stack, Tablet/Mobile 일반 세로 카드, 긴 내용도 잘림 없이 접근 가능 |
| 11 | Contact 페이지 | Shopify 기본 contact form 사용, 담당자명·이메일·문의사항 필수 검증, 성공·실패 메시지와 카카오톡 링크 제공, 개발 스토어에서 실제 수신 확인 |
| 12 | 반응형·접근성·QA | 지정 6개 너비, 키보드·터치·reduced motion·Theme Editor 재로딩 검증, Theme Check 오류 해소, 프리뷰에서 링크·폼·자산 실패 상태 확인 |

About은 2단계에서 메뉴·경로를 계획하고 11단계 이후 QA 전에 소개·파트너·리뷰 section과 템플릿 골격을 준비한다. 최종 소개문·문구·수치가 확정되기 전에는 임의의 실적을 만들지 않고 미입력 블록을 숨긴다. 구조 완료와 콘텐츠 확정을 별도로 기록한다.

## 5. 구현 난도가 높은 항목과 전략

### Hero 우측 SVG/3D 애니메이션

- 루트의 `bnon-universe-3d-planets-bigger.html`은 원본으로 보존한다. 향후 내용을 검토하고 외부 의존성, 임베드 허용 여부, 접근성, 반응형, 성능을 확인한 뒤 SVG/전용 asset 또는 iframe 방식을 결정한다. HTML을 assets에 두는 것만으로 실행 가능하다고 가정하지 않는다.
- 표시 영역 비율과 최소 높이를 확보하여 로딩 중 레이아웃 이동을 줄인다. 정적 이미지가 기본 안전 상태이며 로드 오류·타임아웃·실행 불가 시 `universe_fallback`을 보여준다.
- 장식은 포커스 대상에서 제외하고 본문 정보와 분리한다. 화면 밖·비활성 브라우저 탭에서 정지하며 모션 감소 환경에서는 정적 이미지로 대체한다.
- 완료 판단: 정상 재생, 표시 끄기, 실패 대체, 작은 화면 잘림, 포인터 방해 여부를 모두 확인한다.

### Main Portfolio 탭 전환

- 공개 블록만 탭과 패널로 만들고 section ID와 block ID로 고유 연결을 구성한다. 기본 탭이 숨겨지거나 삭제되면 첫 공개 탭을 선택한다. 공개 탭이 없으면 섹션을 비워두지 않고 숨긴다.
- 하나의 활성 상태로 배경·목업·제목·설명을 갱신한다. 빠른 연속 입력에서도 이전 전환이 선택 결과를 덮어쓰지 않도록 취소·정리한다.
- ARIA tab/tablist/tabpanel, 선택 상태와 방향키·Home/End, Enter/Space를 지원한다. 모바일은 탭 목록을 가로 스크롤하고 선택 탭이 보이도록 한다.
- 다음 탭 힌트는 허용하되 자동 전환은 금지한다. 이미지 로딩 실패 시 제목·설명·문의 동선은 유지한다.

### 프로젝트 미리보기 팝업과 긴 이미지 자동 스크롤

- Home 대표 프로젝트와 Portfolio 갤러리·그리드가 동일한 카드 데이터 계약 및 페이지당 모달 1개를 공유한다. section 간 중복 모달을 만들지 않는다.
- Desktop에서는 데스크톱·모바일 캡처 각각의 로드 완료 후 표시 너비에 따른 높이를 측정하고, 뷰포트 초과 거리만 top → bottom으로 이동한다. 짧은 캡처는 이동하지 않는다. 화면 크기 변경 시 재측정한다.
- 모달이 열리고 브라우저 탭이 보일 때만 재생한다. hover/focus, 사용자 정지, 탭 비활성, 모달 닫기에서 중단한다. 사용자 정지 상태를 이벤트가 임의로 해제하지 않게 한다.
- 정지/재생 버튼과 수동 스크롤 경로를 제공한다. 모바일 화면 및 reduced motion에서는 두 캡처를 정적 첫 화면으로 제공하고 수동 탐색 가능하게 하며, 캡처 종류 선택 UI를 둔다. 여기서 “모바일 캡처”와 “모바일 기기에서의 자동 재생”을 구분한다.
- ESC·닫기 버튼·포커스 가두기·배경 접근 차단·원래 카드로 포커스 복귀·본문 스크롤 복원을 구현한다. 캡처 누락 시 해당 영역에 대체 안내를 표시한다.

### Our Universe 360도 궤도 애니메이션

- `images/our-universe-img` 원본의 향후 업로드 사본을 `orbit_item` 이미지로 연결한다. CSS transform 중심의 회전 궤도와 역회전 이미지로 가독성을 유지하며, JS 제어가 필요한 경우에만 requestAnimationFrame을 쓴다.
- 중심 콘텐츠와 장식 영역을 분리하고 크기·궤도 반경을 화면에 맞춰 제한한다. 이미지 수가 많아도 모든 원본을 동시에 로드하지 않고 운영자가 선택한 항목만 노출한다.
- 정지 제어와 화면 밖·브라우저 비활성 시 정지를 제공한다. 모바일·reduced motion·모션 설정 끄기에서는 겹침 없는 정적 배치로 바꾼다.

### Service sticky 카드 stack 인터랙션

- Desktop에서 `position: sticky`, 순서별 z-index와 상단 간격으로 겹침을 구성한다. 상위 요소의 overflow/transform이 sticky를 깨뜨리는지 점검한다.
- 카드 내용 높이와 Header 높이를 고려해 스크롤 거리를 확보한다. 키보드 포커스를 받은 링크가 앞 카드에 가려지지 않도록 확인한다.
- Tablet/Mobile, reduced motion, 높이가 부족한 가로 화면에서는 일반 문서 흐름의 세로 카드로 전환한다. JavaScript 없이도 서비스 내용과 링크를 읽을 수 있게 한다.

## 6. 반응형 기준

| 구간 | 기준 | 주요 배치 |
| --- | --- | --- |
| Desktop | 1024px 이상, 1440px 중심 | 넓은 Hero, 전체 메뉴, Portfolio 비대칭 그리드, Service stack |
| Tablet | 768~1023px, 768px 중심 | 간격·타이포 축소, 메뉴 접기, Portfolio 2열, Service 일반 카드 |
| Mobile | 767px 이하, 특히 480px 및 375px | 세로 흐름, 좌우 20~24px, Portfolio 1열, 가로 스크롤 탭, 정적 Universe·캡처 |

480px 이하에서는 버튼·폼·모달 패딩과 큰 제목 줄바꿈을 추가 조절한다. QA 너비는 **1440 / 1200 / 1024 / 768 / 480 / 375px**이며 경계 직전·직후도 점검한다. Portfolio Desktop의 large 7 / medium 5는 12열 기준 점유 폭으로 해석하고 small은 한 행 3개로 계획한다. 이는 최종 Figma 확인 시 조정할 설계 가정이다.

## 7. 접근성과 모션 감소

- 올바른 제목 계층, 본문 건너뛰기 링크, 명시적 폼 라벨, 보이는 키보드 포커스와 충분한 터치 영역을 제공한다. 이미지 alt는 편집 가능하게 하고 순수 장식 이미지는 빈 alt로 처리한다.
- 색만으로 활성 상태를 표현하지 않는다. 민트는 밝은 배경의 작은 본문 글자색으로 무리하게 쓰지 않고 다크 텍스트·밑줄·선택 상태를 함께 사용하며 대비를 검증한다.
- hover 정보는 focus와 터치로도 접근할 수 있게 한다. FAQ는 네이티브 details/summary 또는 의미가 동일한 button/panel 구조를 선택한다.
- `prefers-reduced-motion`에서는 Hero 움직임, 궤도 회전, 캡처 자동 스크롤, 진행 바 애니메이션, 부드러운 앵커 스크롤과 stack 전환을 끄거나 즉시 상태 변경으로 대체한다. 콘텐츠 자체는 숨기지 않는다.
- 지속되는 장식 모션과 자동 스크롤에는 사용자 정지 수단을 제공한다. 운영체제 설정이 실행 중 바뀌어도 반영한다.

## 8. Theme Editor 운영 원칙

- 제목·설명·링크·이미지·모션 옵션은 section settings, 프로젝트·서비스·단계·궤도·FAQ·파트너/리뷰는 반복 block으로 분리한다. 구체적인 목록은 `FILE_STRUCTURE.md`를 따른다.
- Main Portfolio 최대 6개, What We Do 최대 5개, Process 최대 6개의 한도를 지키며 초기 콘텐츠는 각각 확보된 프로젝트, 서비스 5종, 프로세스 6단계로 구성한다.
- `published`를 가진 프로젝트는 공개 항목만 출력한다. 순서는 Theme Editor 블록 순서를 사용하고 이미지·링크 미입력 시 깨진 UI가 나오지 않게 한다.
- section 단위로 초기화하고 편집기의 section load/unload, block select에 대응한다. unload에서 이벤트·observer·애니메이션을 정리하고 재초기화 중복을 막는다. 블록 선택 시 해당 탭·패널을 확인할 수 있게 한다.
- section 간 블록은 자동 공유되지 않으므로 같은 프로젝트를 여러 섹션에 넣을 때 제목·캡처·링크 일치 여부를 운영 검수한다. 향후 데이터 중앙화는 별도 범위로 결정한다.

## 9. 최종 검수와 보류 사항

프리뷰에서 Theme Check, 브라우저 콘솔, 키보드/터치, 이미지 실패·미입력·블록 0개 상태, 폼 오류·성공·메일 수신, 카카오톡·외부 링크, 편집기 추가·삭제·순서 변경을 확인한다. 초기 큰 이미지와 폰트의 로딩 우선순위를 검토하고 비핵심 이미지는 지연 로딩하여 모션 때문에 화면이 버벅이거나 레이아웃이 이동하지 않는지 확인한다.

공식 로고, About 최종 소개문, 파트너/리뷰 문구·수치, Main Portfolio 추가 5개 디자인 화면은 대기 항목이다. MONCLOS / ENTALPY / OLLOCDAM / CCLIME은 디자인상의 탭 예시이며 자산 확보·공개 승인과 동일하지 않다. 확인된 콘텐츠만 노출한다.

현재 완료 범위는 계획 문서 작성뿐이다. 테마 도입, 모든 웹 구현, 콘텐츠 업로드, Theme Editor 구성, 문의 전송 및 화면 QA는 아직 실행하지 않았다.
